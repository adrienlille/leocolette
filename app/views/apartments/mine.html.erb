<% @apartments.each do |apartment| %>
  <%= link_to apartment.name, apartment_path(apartment) %>
  [<%= link_to "Delete", apartment_path(apartment),
      method: :delete,
      data: { confirm: "Are you sure?" } %>]
  [<%= link_to "Edit", edit_apartment_path(apartment) %>]
<% end %>
